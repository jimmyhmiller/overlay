# overlay

## Summary

A menu-bar-only macOS annotation overlay written entirely in Coil. It lets a
person mark the screen with boxes, arrows, freehand ink, and text, then exports
an ordered PNG series whose paths can be handed directly to an agent.

Project notes live in [the overlay pad](pad://overlay).
